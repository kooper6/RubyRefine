This is a training ruby tool for simple MARL without retraining models.


I run it on macbook air with 16Gb memory, M3 processor, so pretty resource constrained.

For now i start with simple math

#TODO my next step would be to implement Judge "agent" which would enable it to judge reasoning and provide better rewards


#Note I use faraday lib to work with containers, historically wrong approach, will change it later
